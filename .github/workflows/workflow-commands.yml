name: Workflow Commands
on:
  workflow_dispatch:

jobs:
  workflow-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run a workflow command
        run: echo "Hello from a workflow command"
      - name: Run a workflow command with a parameter
        run: echo "Hello from a workflow command with a parameter" >> $GITHUB_STEP_SUMMARY
